'use babel';

import GenerateFunctionDescriptionView from '../lib/generate-function-description-view';

describe('GenerateFunctionDescriptionView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
